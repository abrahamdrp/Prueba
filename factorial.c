    /* Nombre del archivo:
    /* Descriopcion del archivo:
    /* Autor:
    /* Ultima actualizacion:
    */

#include <stdio.h>
    int main(){
        int n,i, multi=0;

        printf("Ingrese el un numero para realizar el factorial del mismo: ");
        scanf("%i",&n);

        if

        }
    printf("El factorial de %i es: %i",n,multi);


    return 0;
              }
